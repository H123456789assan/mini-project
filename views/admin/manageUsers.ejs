<%- include('../partials/adminHeader') %>

<h1>Manage Users</h1>

<% users.forEach(user => { %>
    <div class="card">
        <p>Name: <%= user.name %></p>
        <p>Email: <%= user.email %></p>
        <p>Role: <%= user.role %></p>
    </div>
<% }) %>

<%- include('../partials/adminFooter') %>